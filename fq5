#include <iostream>
using namespace std;

int digitSum(int n) {
    if (n == 0) return 0;
    return abs(n % 10) + digitSum(n / 10);
}

int main() {
    int n;
    cin >> n;
    cout << digitSum(n);
    return 0;
}